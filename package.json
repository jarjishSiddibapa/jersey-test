{
  "name": "jersey-idea",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "node --import ./scripts/register-ts-resolve.mjs --test src/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "~6.0.2",
    "vite": "^8.3.0"
  }
}
